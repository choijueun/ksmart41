<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="k1.smart.team.mapper.cje.WarehouseMapper">

<select id="getAllWarehouseList" parameterType="String" resultType="Warehouse">
SELECT 
	 w.warehouseCode
	,w.warehouseName
	,w.category1
	,w.category2
	,w.`area`
	,w.currentWeight
	,w.loaction
	,w.outPlace
	,w.maxWeight
	,w.`status`
	,w.regDate
	,w.updateDate
FROM 
	k1_tb_warehouse_info AS w
WHERE
	mainBusinessCode = #{mainBusinessCode};
</select>

</mapper>